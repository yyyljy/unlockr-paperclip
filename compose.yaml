services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: unlockr
      POSTGRES_USER: unlockr
      POSTGRES_PASSWORD: unlockr
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U unlockr -d unlockr"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: unlockr
      MINIO_ROOT_PASSWORD: unlockr-local-secret
    ports:
      - "${S3_API_PORT:-9000}:9000"
      - "${S3_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio-data:/data

  createbuckets:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_started
    restart: "no"
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 unlockr unlockr-local-secret; do sleep 1; done &&
      mc mb --ignore-existing local/unlockr-local
      "

volumes:
  postgres-data:
  redis-data:
  minio-data:
